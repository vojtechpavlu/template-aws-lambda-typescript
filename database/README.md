To start the database with pre-initialized data, enter the following command into console:

```sh
  docker-compose -f docker-compose.local.yml down && docker-compose -f docker-compose.local.yml up --build
```
